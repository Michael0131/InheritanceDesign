#include "gps.h"
#include "simulator.h"
#include "fragment.h"

void GPS::createParts(Simulator& sim)
{
   // when you implement Part types, do:
   // sim.parts.push_back(new GPSCenterPart(pos, v));
   // sim.parts.push_back(new GPSLeftPart(pos, v));
   // sim.parts.push_back(new GPSRightPart(pos, v));
}
